<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema AppCompat (descendiente de AppCompat) -->
    <style name="Theme.JourneyLink" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Opcional: colores de barras -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
